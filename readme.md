Repository where I can play with Qtbridge-Rust. Will be upstreamed to cxx-kde-frameworks eventually.


- `apps/` contains application code to help prototype the library
- `kf-gen` uses upstream qtbridge-rust generator to generate cxx bridges into `kf-type-lib`
- `kf-types` is a handmade bridge to compare against and test out the library
